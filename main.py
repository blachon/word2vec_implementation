import torch

if __name__ == "__main__":
    pass

# TODO get data

# TODO set train and validation corpora

# TODO set optimiwer, lr, criterion

# set device
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

# TODO train
